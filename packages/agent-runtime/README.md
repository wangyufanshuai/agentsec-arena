# Agent Runtime

Future package for attacker and defender orchestration.

MVP behavior lives in `apps/api/agentsec_arena/agents.py` so the demo can run without a worker stack. Real providers should be added behind a provider interface with a mock implementation kept as the default.

