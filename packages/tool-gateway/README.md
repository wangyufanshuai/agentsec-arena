# Tool Gateway

Future standalone package for safety policy enforcement and command execution.

MVP behavior lives in `apps/api/agentsec_arena/safety.py`. Real tool execution should only run after target validation and command allowlist checks.

