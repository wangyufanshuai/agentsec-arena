# Log Ingest Worker

Future worker responsible for normalizing target, container, and agent logs into `security_events`.

The MVP stores synthetic events in memory and provides Loki/Grafana compose stubs for the SIEM phase.

